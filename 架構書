# 專案實作計劃書：輕量級分散式雙軌排程調度系統 (ThreadPool 安全監控版)

## 👤 角色設定
你是一位精通 Windows 核心架構、Python 併發程式設計（Concurrency）與高性能資料庫設計的資深系統架構師。請依據下方明確的環境限制、自訂主機簡寫變數、以及將執行結果寫入日誌供網頁 Dashboard 監控的需求，協助我編寫排程管理調度腳本。

---

## 🛑 1. 執行環境與嚴格限制 (Environment & Constraints)
*   **中央伺服器 (ml350gen8)**: 託管前端網頁面板、FastAPI 後端、以及中央監控 DashBoard。
*   **執行端主機 (A、B、C 多台主機)**: 實際存放並執行各個 Python 小程式，每台主機記憶體僅 **8 GB**。
*   **Python 版本**: **Python 3.7**。嚴禁使用 Python 3.8+ 的新語法。
*   **資安政策**: **絕對禁止下載與安裝任何外部第三方排程管理套件**。
*   **資料庫**: MySQL 5.8.8 (位於中央環境，庫名：`log_record`)。

---

## 🏗️ 2. 核心架構與技術細節
1.  **環境變數自訂**: 在 `Run_Schedule.py` 頂端宣告 `CURRENT_HOST = "Host-A"`，以此簡寫去 MySQL 過濾屬於這台機器的任務。
2.  **多虛擬環境動態指定**: 任務執行時，動態讀取資料庫設定的專屬 venv 路徑（`t.python_executable`）。
3.  **內建 ThreadPool 限制 5 個並行**: 利用 Python 內建的 `concurrent.futures.ThreadPoolExecutor(max_workers=5)` 建立執行緒池。所有到期任務由 5 個櫃檯阻塞式（`subprocess.run`）排隊消化，嚴防記憶體炸裂。
4.  **網頁監控數據回寫機制**: 任務執行結束後，必須精確計算開始時間、結束時間、耗時秒數、執行狀態（SUCCESS/FAIL）。隨後在執行緒內開啟獨立的 DB 連線，**同時完成「更新下次執行時間」與「寫入歷史日誌表（Dashboard 數據源）」**。

---

## 🗄️ 3. 資料庫設計 (三張表核心監控架構)

### 表 1：任務主表 (`loader_task`)
*   `id` (INT, PK, AI)
*   `project_name` (VARCHAR), `task_name` (VARCHAR)
*   `file_path` (VARCHAR), `file_name` (VARCHAR)
*   `python_executable` (VARCHAR(500)) : 該任務指定的 Python venv 絕對路徑
*   `timeout_seconds` (INT) : 逾時時間
*   `is_active` (CHAR(1)) : 'Y'/'N'

### 表 2：時間排程表 (`loader_schedule`)
*   `id` (INT, PK, AI), `task_id` (INT, FK)
*   `schedule_name` (VARCHAR), `schedule_type` (VARCHAR)
*   `interval_minutes` (INT, NULL), `daily_time` (TIME, NULL), `weekly_days` (VARCHAR, NULL)
*   `executor_host` (VARCHAR) : 指定執行的主機簡寫名稱 (例如: Host-A, Host-B)
*   `next_run_at` (DATETIME, INDEX) : 下一次執行的精確時間 (調度器撈取核心依據)
*   `is_active` (CHAR(1)) : 'Y'/'N'

### 表 3：[核心監控] 歷史執行日誌表 (`loader_execution_log`) -> Dashboard 數據源
*   `id` (INT, PK, AI), `schedule_id` (INT)
*   `task_name` (VARCHAR) : 冗餘儲存任務名稱，方便網頁直接撈取
*   `host_name` (VARCHAR) : 執行主機 (Host-A)
*   `start_time` (DATETIME), `end_time` (DATETIME), `duration_seconds` (INT)
*   `status` (VARCHAR) : `SUCCESS` 或 `FAIL`
*   `exit_code` (INT) : 結束碼
*   `error_message` (TEXT) : 存放 Exception 錯誤訊息

---

## 🛠️ 4. 程式碼實作任務 (Copilot Task List)
請幫我產出通用調度主程式 **`Run_Schedule.py`** 的完整程式碼，並包含完整、繁體中文的關鍵註解：

1.  在程式碼最上方提供 `CURRENT_HOST = "Host-A"` 與 `MAX_CONCURRENT_TASKS = 5`。
2.  主執行緒連線至 MySQL，透過 `INNER JOIN` 撈出目前屬於本機簡寫且 `s.next_run_at <= NOW()`、且兩張表的 `is_active` 皆為 'Y' 的任務清單。撈完立刻關閉主連線。
3.  使用 `ThreadPoolExecutor(max_workers=MAX_CONCURRENT_TASKS)` 併發處理清單。
4.  在 Work 執行緒函數中：
    *   計算開始時間，使用 `subprocess.run` 執行子任務，捕捉 `returncode`。若拋出異常，狀態標記為 `FAIL` 並記錄錯誤文字。
    *   計算結束時間與耗時。**隨後開啟獨立 DB 連線**，將下一次執行時間更新回 `loader_schedule.next_run_at`，並將所有執行數據（包含 error_message）`INSERT` 寫入 `loader_execution_log`。
5.  確保所有排隊任務消化完畢後，調度器才完整釋放退出。

---

## ⚙️ 5. 程式碼編寫風格範本 (Code Style Reference)
為了保持專案結構的一致性，請務必嚴格參照我現有的程式碼撰寫風格、命名邏輯、以及資料庫封裝習慣來產出全新的代碼。
